<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BuddyGram</title>
    {{!-- stylesheets and materialize --}}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.slim.min.js"></script>
         <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</head>
<body>
    {{!--  nav-bar(s)--}}
    <header>
    <nav>
        <div class="nav-wrapper">
    {{!-- App name/logo --}}
        <a href="#" class="logo">BuddyGram</a>
        <img src="/images/BuddyGram.JPG"> 
    {{!-- Nav bar links when a user is logged in --}}
        {{#if logged_in}}
        <a href="/profile">Profile</a> |
        <button class="no-button" id="logout">Logout</button>
    {{!-- What a user will see in the nav if they are not logged in--}}
        {{else}}
        <ul id="nav-mobile" class="right">
        <li><a href="/login">Login</a></li>
        <li><a href="/signup">Sign Up</a></li>
        <li> <a href="/aboutus">About Us</a></li>
        </ul>
        {{/if}}
        </div>
    </nav>
    </header>

    {{!-- page content --}}
   {{{body}}}

    {{!-- Script for logged in users --}}
      {{#if logged_in}}
  <script src="/js/logout.js"></script>
      {{/if}}

    {{!-- linking jquery --}}
        
</body>
</html>